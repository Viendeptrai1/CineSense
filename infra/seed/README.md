# Seed dữ liệu SQLite

```bash
python scripts/seed_sqlite_from_csv.py
```

Tạo `data/cinesense.db` từ `Notebook_Report/cinesense_*.csv` và `movie_index.json` trong artifact (xem `scripts/seed_sqlite_from_csv.py`).
